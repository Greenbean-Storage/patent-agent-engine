"""lint 의 4 runner 모듈.

ruff        — lint + format 자동적용 (ruff check --fix + ruff format, write)
mypy        — type check
bandit      — code security pattern (SAST)
pip_audit   — dependency CVE (pyproject.toml + uv.lock)
"""
