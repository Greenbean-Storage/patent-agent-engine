"""actor:fake mock 앱 — via:image knob actor=fake (ACTOR_TARGET=mock) 일 때 기동.

실 Actor 외부 표면(/health · /dispatch SSE fixture replay · /tool canned · busy-503)의
mock 구현. fixture 키잉 = 실 CM read. 모듈 구성은 `app.py` docstring 참조.
"""

__version__ = "0.1.0"
